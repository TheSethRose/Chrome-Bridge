interface:
  display_name: "X via Chrome Bridge"
  short_description: "Retrieve X data and operate the signed-in account"
  default_prompt: "Use $x-chrome-bridge to retrieve my X data or operate my signed-in account for this task."
